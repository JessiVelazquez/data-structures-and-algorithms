'use strict'

class Node {
  constructor(name) {
    this.name = name;
    this.next = null;
  }
}

class LinkedList {
  constructor() {
    this.head = null;
  }
}

let duckDuckGoose = (list, k) => {
  let Joe = new Node(Joe);
  let Karen = new Node(Karen);
  let Brian = new Node(Brian);
  let Steph = new Node(Steph);
  let Max = new Node(Max);

  let list = new LinkedList()

}

let duckDuckGoose = (list, k) => {
  let answer = '';
  while(list.head){

  }
}
